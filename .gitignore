/bin/
agent-server
.env

# macOS
.DS_Store
**/.DS_Store

# local docs (not committed)
/docs/

# build output (SDK, frontend, etc.)
dist/

# go:embed needs a tracked file in cmd/dist so CI compiles without a frontend build.
!cmd/dist/
cmd/dist/*
!cmd/dist/.gitkeep
node_modules/
